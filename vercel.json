{
  "installCommand": "npm install && cd server && npm install && cd ../client && npm install && chmod +x node_modules/.bin/tsc node_modules/.bin/vite",
  "buildCommand": "cd client && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
